# MikeVideo YouTube-parity roadmap — autonomous build progress

**This file is the source of truth for the 30-min build loop.** Each run: finish any `IN_PROGRESS`
phase, else start the first `PENDING` one; implement it end-to-end (cloud + web + app where relevant),
**deploy**, **test (chrome-pool for web, adb for the app)**, commit both repos, then mark it `DONE`
(or `FAILED: <reason>`). One phase per run. Never touch other phases. Don't disturb the OSM stack.

Status legend: `PENDING` · `IN_PROGRESS` · `DONE` · `FAILED`

| Phase | Status | Scope (build across cloud + web + app; test all touched surfaces) |
|---|---|---|
| **P1** | DONE | **Engagement primitives.** Migration: `views`(count via a `video_views` event or a counter col), `likes`(video_id,user_id unique), `watch_progress`(video_id,user_id,position_sec,updated). Endpoints: `POST /api/videos/{id}/view`, `POST/DELETE /api/videos/{id}/like`, `GET/PUT /api/videos/{id}/progress`; add `views/likes/liked/progress` to video detail+list. Web: show view count + age on cards, like button + count on watch, **resume** playback from saved position + a "Continue watching" row. App: like button, resume, report views/progress. |
| **P2** | DONE | **Playlists + Watch Later / Save.** Migration: `playlists`, `playlist_items`. CRUD endpoints + a built-in "Watch Later". Web: Save-to-playlist menu on cards/watch, a Playlists page, Watch Later. App: save menu + playlists. |
| **P3** | DONE | **Channels (@handle) + public profile.** Migration: `channels`(user_id, handle unique, display_name, avatar, bio) or reuse user; `GET /api/channels/{handle}` (public: their public videos + counts). Web: a channel page (avatar, name, subscriber count, video grid), channel name+avatar on cards linking to it. App: channel view. |
| **P4** | DONE | **Subscriptions.** Migration: `subscriptions`(subscriber_user_id, channel_user_id). Subscribe/unsubscribe endpoints + `GET /api/feed/subscriptions`. Web: Subscribe button on channel/watch, a left sidebar (Home/Shorts/Subscriptions/Library), a Subscriptions feed. App: subscribe + subs feed. |
| **P5** | DONE | **Comments.** Migration: `comments`(video_id,user_id,parent_id,text,hearts,likes,created). Threaded CRUD + like/heart. Web: comments section on watch (post, reply, like, heart-by-owner, sorted). App: comments UI. |
| **P6** | DONE | **Personalized home feed / recommendations.** `GET /api/feed/home`: blend your subs' recent + popular (views/likes) + tag-affinity from your watch history/likes; fall back to public. Web: home becomes the recommended feed for signed-in users. App: recommended home. |
| **P7** | DONE | **Related / up-next + Autoplay.** `GET /api/videos/{id}/related` (tag/transcript similarity + popularity). Web: replace the library rail with real related + an autoplay-next toggle. App: related + autoplay. |
| **P8** | DONE | **Notifications.** Migration: `notifications`(user_id,type,payload,read,created). Fan-out on new upload to subscribers. `GET /api/notifications` + mark-read. Web: a bell with unread count + dropdown. App: notifications (+ MikeOS notification/hive if wired). |
| **P9** | PENDING | **Shorts.** `GET /api/shorts` (portrait, short-duration public videos). Web: a vertical full-screen swipe feed. App: a Shorts swipe pager. (Portrait encoding already correct.) |
| **P10** | PENDING | **Player polish.** Web: quality selector (HLS levels via hls.js), playback-speed menu, miniplayer (keep playing while browsing), theater mode, keyboard shortcuts. App: speed + quality + PiP/background. |
| **P11** | PENDING | **Trending / Explore.** `GET /api/explore` + `?tag=`: popular public videos, categories from AI tags. Web: an Explore page with tag chips. App: explore. |
| **P12** | PENDING | **Creator Studio (analytics).** `GET /api/studio/overview` + per-video stats: views over time, watch-time (from progress), likes, top videos. Web: a Studio dashboard. App: a lightweight studio view. |
| **P13** | PENDING | **Custom thumbnails + light edit.** Pick a frame or upload a thumbnail (`POST /api/videos/{id}/thumbnail`); optional trim (re-encode a subclip). Web: thumbnail picker on watch/studio. App: thumbnail picker. |
| **P14** | PENDING | **Embeds + external share.** An embeddable player (`GET /embed/{id}` for public videos) + oEmbed (`GET /api/oembed`). Web: a Share→Embed dialog with copyable iframe. (App: n/a.) |

## Run log (append one line per run)
- P8 DONE (2026-07-27): notifications. Cloud: migration 011_notifications (kind/is_read/jsonb payload). Fan-out on first-publish→new_video to subscribers, new comment→video owner, new subscriber→channel owner. GET /api/notifications (+unread_count), POST /api/notifications/read (one/all). Web: header bell + unread badge + dropdown (thumb/text/age, click→mark-read+navigate, mark-all, 45s poll). App (v0.11.0): bell w/ BadgedBox unread count → NotificationsScreen (thumb+text+age+unread dot+mark-all, tap deep-links). Tested: API via X-API-KEY (chrome-pool was DOWN — SSL handshake timeout — so web-UI drive skipped, but verified the exact API the web calls): GET returned 3 kinds + unread=3, publishing a private video fanned out new_video to a synthetic subscriber, mark-one→2, mark-all→0; adb Note10 — bell badge '2', NotificationsScreen shows both notifications w/ thumb + unread dots, Mark-all-read cleared them (server unread=0). Test data cleaned + video reverted to private. Both repos deployed; health 200; player still known-good.
- P7 DONE (2026-07-27): related/up-next + autoplay. Cloud: GET /api/videos/{id}/related — ranks watchable videos by tag overlap(*10) + same-channel(+8) + popularity(views cap + likes*3) + recency; public + viewer's own, excludes source. Web: watch rail now real 'Up next' (channel+views) + Autoplay toggle (persisted, default on); autoplay-next hooks ONLY the video 'ended' event → go to top related (never touches mountPlayer decode/HLS — kept the reverted known-good player). App (v0.10.0): related fetched on open; 'Up next' list + Autoplay Switch in the ⓘ panel; ExoPlayer Player.Listener STATE_ENDED → playNextIfAuto (observes state only). Tested: chrome-pool — related=17 (scored, excludes source), Up-next 17 rows + toggle default-on + persisted, simulated 'ended' navigated to related[0]; adb Note10 — ⓘ panel shows Up-next list (AI titles/durations) + Autoplay ON, and autoplay-next fired when a short clip ended. Both repos deployed; health 200.
- P6 DONE (2026-07-27): personalized home feed. Cloud: GET /api/feed/home (no migration) — scores PUBLIC ready videos by subscriptions(+50) + tag-affinity from liked/watched(+20/tag) + popularity(views cap + likes*5), ordered by score, each card tagged with a reason (From your subscriptions / Based on what you watch / Popular / New); falls back to recent public. Web: signed-in home = Recommended feed + 'Your videos' tab + Continue-watching, cards show the reason (✦). App (v0.9.0): a Recommend icon → 'For You' FeedScreen (generalized from SubsFeedScreen). Tested: chrome-pool signed-in — /api/feed/home returned 3 scored videos (9-view clip first, reasons Popular/New), home tabs Recommended(active)/Your-videos with 3 reason labels + 18 in Your-videos; adb Note10 — For You screen renders the 3-video grid. Both repos deployed; health 200. (Player left at the reverted known-good version.)
- P0 REVERT (2026-07-27): the P0 playback "fix" below (autoplay + 6s watchdog + MP4-swap) REGRESSED real desktop-Chrome playback — user confirmed all videos played ~2-3h earlier and then none did. Root cause: the watchdog could call hls.destroy()+swap sources on a healthy stream, and none of that logic was validatable in chrome-pool (headless Chromium has NO H.264 decoder, so it "fails" regardless — I was blind to the real-browser effect). Reverted mountPlayer to the simple native-HLS/hls.js/MP4 selection. Kept `Cache-Control: no-cache` so the revert propagates. Verified deployed: watchdog code gone, JS valid, player mounts hls blob with no JS errors. LESSON: do NOT ship player-logic changes that can't be decode-tested; the headless tool cannot validate H.264 playback.
- P0 FIX (2026-07-27): user reported videos wouldn't play on a desktop browser (even clicking play). Media verified perfect (phone plays; ffprobe = standard H.264 High/yuv420p + AAC-LC; segments 200/206) → client-side hls.js stall / stale cached SPA. Fixed web player (`mountPlayer`): robust autoplay (muted fallback), hls.js error recovery, incompatible-codecs→MP4, and a 6s WATCHDOG that hard-switches to the progressive MP4 if HLS produces no playable data. Added `Cache-Control: no-cache` on the SPA HTML so fixes reach users. Validated fallback chain fires in chrome-pool (currentSrc→MP4); full decode-playback unverifiable there (headless Chromium has NO H.264 decoder) — asked user to hard-refresh + confirm.
- P5 DONE (2026-07-27): comments — threaded (1 reply level), per-viewer likes, owner heart. Cloud: migration 010_comments + comment_likes; GET/POST comments (public→anyone, private→owner), DELETE (author or owner), like + heart endpoints. Web: watch-page comments section (composer, replies, like toggle+count, creator heart, delete, N-comments header). App (v0.8.0): comment icon in the player top bar → ModalBottomSheet (compose, post, like/reply/heart/delete). Tested: chrome-pool signed-in — post→node, list count, like {liked,1}, reply parent_id, heart {hearted:true}, watch DOM "2 Comments" + liked state, delete→200 cascade→count 0; adb Note10 — sheet opens, posted "1 Comments · Mike Westoo" with actions, Delete → server 0 rows. Both repos deployed; health 200.
- P4 DONE (2026-07-26): subscriptions. Cloud: migration 009_subscriptions + POST/DELETE /api/channels/{handle}/subscribe (video.write; self-sub→400) + GET /api/feed/subscriptions; channel/video detail carry subscriber_count/subscribed/is_me; shared _public_card/_CARD_SELECT. Web: left icon rail (Home/Subs/Library) + Subscriptions header btn, #subs feed page, Subscribe/Subscribed toggle w/ live count on channel + watch byline. App (v0.7.0): Subscriptions icon → SubsFeedScreen, Subscribe pill + subscriber count on ChannelScreen. Tested: chrome-pool signed-in — self-sub 400, subscribe→{true,1}, channel shows subscribed+count, subs feed 200, channel page button "Subscribed ✓", unsubscribe→{false,0} (used a synthetic 'testcreator' channel, deleted after); adb Note10 — Subscriptions nav → SubsFeedScreen empty-state renders. NOTE: app Subscribe button/subs-feed population need a 2nd creator with public videos (none exists in this single-user system yet) so weren't visually exercised on-device — the subscribe client/VM path is the same one verified green on web. Both repos deployed; health 200.
- P3 DONE (2026-07-26): channels (@handle) + public creator profiles. Cloud: migration 008_channels (1/user, case-insensitive unique handle), GET /api/channels/{handle} (public profile + public videos + counts), GET/PUT /api/channels/me (auth; edit handle/name/bio/avatar), channel brief on public feed cards + video detail, _ensure_channel on upload + idempotent startup backfill. Web: #channel page (avatar/name/@handle/stats/bio + grid), edit modal (+ sign-out moved there), byline on cards + watch page, account avatar → your channel. App (v0.6.0): channel byline in the ⓘ panel → ChannelScreen (header + public-video grid). Tested: curl (public 200, case-insensitive/@, unknown 404, me 401); chrome-pool signed-in (GET/PUT me, channel page "Mike Westoo · @u690a994215 · 3 videos · 2 views" + bio + edit, watch byline); adb Note10 (info-panel byline → channel screen w/ 3-video grid). Both repos deployed; health 200.
- P0 FIX (2026-07-26): video.osmike.com + up.osmike.com were DOWN (public 000; cloud container healthy internally at 172.17.0.1:8090 → 200). Root cause: mikeos-caddy restarted ~13min earlier and loaded /root/mikeos-osm/Caddyfile which had LOST the video./up. site blocks (only osm./tiles. remained). Fix: appended the two blocks (video→172.17.0.1:8090, up→172.17.0.1:8080 with 20GB body) + `caddy reload`; verified 200. Backup Caddyfile.bak.* on the box. NOT caused by the loop's deploys (they never touch Caddy). This fire spent on the outage — P3 stays PENDING for the next run. RECOMMEND: commit the video/up blocks into the mikeos-osm Caddyfile git so a future restart can't drop them again.
- P2 DONE (2026-07-26): playlists + Watch Later (migration 007) + CRUD/items endpoints; web Playlists list+detail pages + "➕ Save" menu; app Save-to-Watch-Later button (v0.5.0). Tested: cloud API end-to-end with a real OAuth token (create playlist, add item, list w/ counts, ?video_id `has` flag, detail all correct); app screenshot shows the Save button. NOTE: chrome-pool was DOWN this run (health 000) so the web UI drive was skipped — verified the API the web calls instead. Both repos deployed.
- P1 DONE (2026-07-26): views+likes+watch_progress (migration 006) + endpoints; web view counts/like/resume/"Continue watching"; app like/resume/view (v0.4.0). Tested: chrome-pool login→like toggle 0→1 + view counted (DB confirmed); adb Note10 → like button shows liked state (cross-surface). Both repos deployed.
